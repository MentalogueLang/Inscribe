[package]
name = "inscribe-incremental"
version = "0.1.0"
edition = "2021"

[dependencies]
inscribe-session = { path = "../inscribe-session" }
serde = { version = "1.0", features = ["derive"] }
bincode = "1.3"
